<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Debug-only. Serves a file over a pipe so the non-seekable path has something to
             exercise; it lives here rather than in androidTest so it runs in the app's own process
             with the app's classpath. -->
        <provider
            android:name=".datasource.image.PipeProvider"
            android:authorities="${applicationId}.debug.pipe"
            android:exported="false" />
    </application>

</manifest>
